# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com

# The lines below are called `modelines`. See `:help modeline`
# Feel free to remove those if you don't want/need to use them.
# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
    # you may remove this if you don't need go generate
    # - go generate ./...

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
      - 386
    ignore:
      - goos: darwin
        goarch: "386"
    binary: terraform-provider-kind_{{.Version}}
    main: ./main.go
    # Ensure consistent naming for Terraform provider
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

archives:
  - formats: [zip]
    # Use the standard Terraform provider naming convention
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # Include the binary and any other necessary files
    files:
      - README.md
      - LICENSE

checksum:
  name_template: 'terraform-provider-kind_{{.Version}}_SHA256SUMS'
  algorithm: sha256
signs:
  - artifacts: checksum
    args:
      # if you are using this is a GitHub action or some other automated pipeline, you
      # need to pass the batch flag to indicate its not interactive.
      - "--batch"
      - "--local-user"
      - "{{ .Env.GPG_FINGERPRINT }}" # set this environment variable for your signing key
      - "--output"
      - "${signature}"
      - "--detach-sign"
      - "${artifact}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# Generate manifest for Terraform Registry
metadata:
  mod_timestamp: '{{ .CommitTimestamp }}'

release:
  extra_files:
    - glob: terraform-registry-manifest.json
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
